/**
 * Simple unit tests for OAuth callback handler using Node test runner
 * Validates: Requirements 1.4, 6.1
 *
 * NOTE: This file uses Node's built-in test runner (node:test).
 * Run with: node --test app/auth/callback/route.simple.test.ts
 * It is intentionally excluded from vitest (see vitest.config.ts exclude list).
 */
